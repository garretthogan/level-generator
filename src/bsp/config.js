export const DEFAULT_CONFIG = Object.freeze({
  dungeonWidth: 80,
  dungeonHeight: 60,
  maxDepth: 5,
  minPartitionSize: 14,
  roomPadding: 2,
  minRoomSize: 8,
  corridorWidth: 2,
  exitWidth: 2,
  seed: 42,
});
